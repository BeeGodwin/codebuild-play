version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - echo "installing stuff"
  pre_build:
    commands:
      - echo "pre-build commands"
  build:
    commands:
      - echo "build commands"
  post_build:
    commands:
      - echo "post-build commands"